// https://github.com/SublimeTextIssues/Core/issues/1640
// Guess settings from buffer: Detected as Tab/4 but should be Space/2

function(keys, values, rereduce) {
  keys.forEach(function(key) {

  })
}
