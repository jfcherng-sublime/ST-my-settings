// These settings override both User and Default settings for the HTML syntax
{
    // "-" is not a word breaker
    "word_separators": "./\\()\"':,.;<>~!@#$%^&*|+=[]{}`~?",
    "tab_size": 2,
    // disable built-in tag autocompletion?
    "disable_default_completions": false,
    // "default_completions_selector": "",
}
